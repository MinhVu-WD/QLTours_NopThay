

@model IEnumerable<QLTours.Models.tour>

@{
    ViewBag.Title = "Tra cứu thông tin khách hàng   ";
    Layout = "~/Views/Shared/_Layout.cshtml";

}

<h2>Tra cứu thông tin đoàn </h2>
<br />
<p>


    <!-- 1. Đoạn cần thêm -->
    @using (Html.BeginForm("tracuuthongtin", "tours", FormMethod.Get))
    {
    <p>
        Tìm theo ID tour: @Html.TextBox("SearchString") <input type="submit" value="Tìm kiếm" />
    </p>
}
    <!-- Kết thúc -->
    </p><br />

    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ten)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdLoaiTour)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdGiaTour)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MoTa)
            </th>



            <!-- Kết thúc -->


            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Ten)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdLoaiTour)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdGiaTour)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.MoTa)
                </td>


                <!-- Kết thúc -->



            </tr>
        }

    </table>